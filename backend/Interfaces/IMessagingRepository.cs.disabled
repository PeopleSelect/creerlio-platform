using Creerlio.Domain.Entities;

namespace Creerlio.Application.Interfaces
{
    public interface IMessagingRepository
    {
        Task<List<ChatThread>> GetThreadsByUserIdAsync(Guid userId);
        Task<ChatThread?> GetThreadByIdAsync(Guid threadId);
        Task<ChatThread> CreateThreadAsync(ChatThread thread);
        Task<List<ChatMessage>> GetMessagesByThreadIdAsync(Guid threadId);
        Task<ChatMessage> SendMessageAsync(ChatMessage message);
        Task SaveChangesAsync();
    }
}
